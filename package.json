{
  "name": "lua-js",
  "version": "0.1.0",
  "description": "lua bindings for node",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "author": "Chris Bajorin <chris@chrisbajorin.com>",
  "license": "MIT",
  "scripts": {
    "test": "tsc && npm install && node ./lib/index.js",
    "install": "node ./build --release"
  },
  "dependencies": {
    "rimraf": "^3.0.2"
  },
  "devDependencies": {
    "@types/node": "^12.19.4",
    "@types/rimraf": "^3.0.0",
    "typescript": "^4.0.5"
  }
}
